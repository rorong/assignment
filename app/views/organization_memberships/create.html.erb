<div class="container mt-4">
  <div class="alert alert-success">
    <h4><i class="fas fa-check-circle me-2"></i>Membership Request Sent!</h4>
    <p>Your request to join <strong><%= @organization.name %></strong> has been submitted.</p>
    <p><small><i class="fas fa-envelope me-1"></i>A notification email would be sent to organization admins in production.</small></p>
    <%= link_to 'Back to Organizations', organizations_path, class: 'btn btn-primary' %>
  </div>
</div> 